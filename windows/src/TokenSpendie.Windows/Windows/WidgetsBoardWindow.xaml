<Window x:Class="TokenSpendie.Windows.Windows.WidgetsBoardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TokenSpendie.Windows.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:DetailPanelViewModel}"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" Topmost="True"
        Width="520" SizeToContent="Height" WindowStartupLocation="Manual"
        Foreground="#EAEAEC">
  <Window.Resources>
    <Style x:Key="HeaderText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#EAEAEC"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="FontSize" Value="15"/>
    </Style>
    <Style x:Key="SubtleText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#EAEAEC"/>
      <Setter Property="Opacity" Value="0.6"/>
      <Setter Property="FontSize" Value="11"/>
    </Style>
    <Style x:Key="PercentText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#EAEAEC"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="FontSize" Value="22"/>
    </Style>
    <Style x:Key="WindowLabel" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#EAEAEC"/>
      <Setter Property="FontSize" Value="12"/>
    </Style>
    <Style x:Key="WindowDetail" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#EAEAEC"/>
      <Setter Property="Opacity" Value="0.55"/>
      <Setter Property="FontSize" Value="11"/>
    </Style>
    <SolidColorBrush x:Key="CardBg" Color="#2A2A30"/>
    <SolidColorBrush x:Key="CardBorder" Color="#3A3A42"/>
    <SolidColorBrush x:Key="BarTrack" Color="#3A3A42"/>
    <SolidColorBrush x:Key="BarFill" Color="#D87856"/>

    <Style x:Key="UsageBar" TargetType="ProgressBar">
      <Setter Property="Height" Value="6"/>
      <Setter Property="Minimum" Value="0"/>
      <Setter Property="Maximum" Value="100"/>
      <Setter Property="Foreground" Value="{StaticResource BarFill}"/>
      <Setter Property="Background" Value="{StaticResource BarTrack}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ProgressBar">
            <Grid>
              <Rectangle RadiusX="3" RadiusY="3" Fill="{TemplateBinding Background}"/>
              <Border x:Name="PART_Track"/>
              <Rectangle x:Name="PART_Indicator" RadiusX="3" RadiusY="3"
                         Fill="{TemplateBinding Foreground}" HorizontalAlignment="Left"/>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border CornerRadius="14" Background="#E61E1E22" Padding="0" BorderBrush="#33FFFFFF" BorderThickness="1">
    <Border.Effect>
      <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.55"/>
    </Border.Effect>
    <StackPanel Margin="20">
      <Grid Margin="0 0 0 14">
        <TextBlock Text="Token Spendie" Style="{StaticResource HeaderText}" FontSize="16"/>
        <TextBlock x:Name="DateText" HorizontalAlignment="Right" Style="{StaticResource SubtleText}" FontSize="12"/>
      </Grid>

      <ItemsControl ItemsSource="{Binding Rows}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Background="{StaticResource CardBg}" CornerRadius="10" Padding="14"
                    Margin="0 0 0 10" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
              <StackPanel>
                <Grid Margin="0 0 0 8">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DisplayName}" Style="{StaticResource HeaderText}"/>
                  </StackPanel>
                  <TextBlock HorizontalAlignment="Right"
                             Text="{Binding HeadlinePercent, StringFormat={}{0:F0}%}"
                             Style="{StaticResource PercentText}"/>
                </Grid>

                <ItemsControl ItemsSource="{Binding Windows}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Grid Margin="0 3">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="120"/>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="48"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                          <TextBlock Text="{Binding Label}" Style="{StaticResource WindowLabel}"/>
                          <TextBlock Text="{Binding Detail}" Style="{StaticResource WindowDetail}"/>
                        </StackPanel>
                        <ProgressBar Grid.Column="1" VerticalAlignment="Center" Margin="0 6 12 6"
                                     Value="{Binding Window.Percent, Mode=OneWay}"
                                     Style="{StaticResource UsageBar}"/>
                        <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"
                                   Text="{Binding Window.Percent, StringFormat={}{0:F0}%}"
                                   Style="{StaticResource WindowLabel}"/>
                      </Grid>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

      <TextBlock Text="{Binding LastRefreshedLabel}" Style="{StaticResource SubtleText}"
                 Margin="2 6 0 0"/>
    </StackPanel>
  </Border>
</Window>
